- name: Provision Linode Server and Deploy Docker Container
  hosts: "{{ lookup('env', 'LINODE_HOST') }}"  # Dynamically use the host from the GitHub secret
  become: true
  gather_facts: false
  vars:
    ansible_ssh_private_key_file: "{{ lookup('env', 'SSH_PRIVATE_KEY') }}"  # Dynamically use the private key from GitHub secret
    ansible_user: root
    ansible_port: 22

  tasks:
    - name: Update and upgrade all packages
      ansible.builtin.apt:
        update_cache: yes
        upgrade: dist

    - name: Ensure essential packages are installed
      ansible.builtin.apt:
        name:
          - curl
          - git
          - unzip
          - software-properties-common
        state: present

    - block:
        - name: Check if Docker is installed
          ansible.builtin.command:
            cmd: docker --version
          register: docker_check
          ignore_errors: yes
          changed_when: false

        - name: Install Docker if not installed
          ansible.builtin.apt:
            name: docker.io
            state: present
          when: "'docker' not in docker_check.stdout"

        - name: Start and enable Docker service
          ansible.builtin.systemd:
            name: docker
            state: started
            enabled: yes

    - name: Log into Docker Hub
      community.docker.docker_login:
        username: "{{ lookup('env', 'DOCKER_USERNAME') }}"
        password: "{{ lookup('env', 'DOCKER_PASSWORD') }}"
      no_log: true

    - name: Pull Docker image from Docker Hub
      community.docker.docker_image:
        name: "{{ image_tag }}"
        source: pull

    - name: Run Docker container
      community.docker.docker_container:
        name: easydrive
        image: "{{ image_tag }}"
        state: started
        restart_policy: always
        published_ports:
          - "80:80"
  handlers:
    - name: Start Docker service
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: yes
